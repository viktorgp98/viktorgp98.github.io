---
/* import { getCollection } from "astro:content";

const experience = await getCollection("experience");
console.log("algo", experience); */
---

<section
  id="experience"
  class="section relative secc-experience bg-primary-light h-screen"
>
  <div class="container mx-auto p-5 md:p-10">
    <!-- <h2
      class="text-[clamp(1rem, calc(5vw + .6rem), 4rem)] font-bold italic text-shadow-md uppercase"
    > -->
    <h2 class="section-title uppercase">Experience /</h2>
    <div></div>
  </div>
</section>
