---
import GradientText from "@/components/gradientText/GradientText.jsx";
import { HoverButton } from "@/components/button/HoverButton.jsx";

const sites = [
  {
    link: "/documents/CV-ViktorGonzalez-2025.pdf",
    title: "CV",
    icon: "icon-[ph--read-cv-logo-duotone]",
    download: "cv_viktorgonzalez.pdf",
  },

  {
    link: "https://www.linkedin.com/in/viktorgonzalezp/",
    title: "LinkedIn",
    icon: "icon-[ph--linkedin-logo-duotone]",
  },
  {
    link: "https://github.com/viktorgp98",
    title: "Github",
    icon: "icon-[ph--github-logo-duotone]",
  },
];
---

<section class="section secc-hero relative h-screen w-full">
  <div
    class="absolute inset-0 bg-cover bg-center bg-fixed bg-no-repeat flex md:items-center justify-center bg-grey w-full h-full"
    style="background-image: url('/images/banner.webp');"
  >
    <div
      class="container mx-auto p-5 md:p-11 absolute z-10 flex flex-col items-start text-white gap-10"
    >
      <!-- available -->
      <div
        class="available absolute top-0 right-0 flex-row flex justify-end items-center gap-2 text-sm text-zinc-400"
      >
        <div class="size-1.5 rounded-full bg-green-400 z-10"></div>
        <span class="text-xs">Disponible para trabajar</span>
      </div>
      <!-- title -->
      <div class="flex flex-col gap-2 md:gap-5 items-start justify-start">
        <h4 class="text-[clamp(1rem, calc(4vw + .5rem), 4rem)]">
          Desarrollador Full Stack Junior
        </h4>
        <GradientText
          colors={["#f26bb5", "#5d5f8c", "#f26bb5", "#5d5f8c"]}
          animationSpeed={7.5}
          showBorder={false}
        >
          <h1 class="text-[clamp(2rem,calc(7vw+1rem),5rem)]/none bold">
            VIKTOR GONZÁLEZ PINTO
          </h1>
        </GradientText>

        <h5 class="text-[clamp(1rem, calc(4vw + .5rem), 4rem)] italic">
          Apasionado por la Innovación Web
        </h5>
      </div>
      <!-- enlaces  -->
      <div
        class="call-to-action flex flex-col md:flex-row gap-10 items-center align-middle"
      >
        <div
          class="buttons flex items-center justify-center md:justify-start gap-4 md:gap-x-4"
        >
          <!-- projects -->
          <HoverButton>
            <a href="#projects" onclick="lenis.scrollTo('#contact')">
              Ver proyectos
            </a>
          </HoverButton>
          <!-- contact -->
          <a
            href="#contact"
            onclick="lenis.scrollTo('#contact')"
            class="justify-center whitespace-nowrap
          rounded-md text-sm font-medium ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring
          focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0
          border border-input bg-background h-10 px-4 py-2 group transition-all duration-200 hover:scale-105 hover:bg-primary hover:text-primary-foreground
          flex items-center gap-2"
          >
            <span class="flex items-center gap-2 font-semibold text-lg">
              Contacto
              <span
                class="icon-[carbon--send] h-4 w-4 transition-transform duration-100 group-hover:translate-x-1"
                role="img"
                aria-hidden="true"></span>
            </span>
          </a>
        </div>
        <div
          class="social-links flex items-center justify-center md:justify-end"
        >
          {
            sites.map((site) => (
              <a href={site.link} download={site.download} target="_blank">
                <title>{site.title}</title>
                <span
                  class={`${site.icon} text-primary icon-size px-10 hover:scale-125 hover:bg-primary transition-all duration-200`}
                  role="img"
                  aria-hidden="true"
                />
              </a>
            ))
          }
        </div>
      </div>

      <div
        class="absolute inset-x-0 bottom-0 top-150 md:top-140 flex flex-row gap-x-5 items-center justify-center pt-10"
      >
        <a href="#skills" onclick="lenis.scrollTo('#skills')">
          <title>Siguiente seccion</title>
          <span
            class="icon-[ph--arrow-circle-down-duotone] w-10 h-10 animate-[sink_1s_ease-in-out_infinite]"
            role="img"
            aria-hidden="true"></span>
        </a>
      </div>
    </div>
  </div>
</section>
